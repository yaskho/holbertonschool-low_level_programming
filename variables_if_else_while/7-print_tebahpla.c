#include <stdio.h>

/**
*prints the lowercase alphabet in reverse
*followed by a new line by putchar
*return: 0
*/

int main(void)
{
	char letter;
	for (lettre = 'z'; letter <= 'a'; letter--) 
	{

	putchar(letter);

	}
	putchar('/n');
	return (0);
}
